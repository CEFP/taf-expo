import {StyleSheet} from "react-native";

const style = StyleSheet.create({

    button: {backgroundColor: "silver", padding: 10, margin: 5},
    buttonText:{textAlign:"center"}
});

export default style
